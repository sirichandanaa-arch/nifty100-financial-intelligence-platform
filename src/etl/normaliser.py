from src.common import norm_year, norm_ticker

def normalize_year(value):
    """Public alias for year normalization."""
    return norm_year(value)

def normalize_ticker(value):
    """Public alias for ticker normalization."""
    return norm_ticker(value)
